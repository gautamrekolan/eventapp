<div class="user">
	<div class="pic">
		<% if user.avatar? %>
			<%= image_tag user.avatar.path(:smaller)%>
		<% else %>
			<%= image_tag '/assets/users/icon-smaller.png' %>
		<% end %>
	</div>
	<div class="username">
		<%= link_to user.username, user %>
	</div>
</div>
