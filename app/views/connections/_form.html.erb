<%= form_for(@connection) do |f| %>
  <% if @connection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@connection.errors.count, "error") %> prohibited this connection from being saved:</h2>

      <ul>
      <% @connection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asking_user_id %><br />
    <%= f.text_field :asking_user_id %>
  </div>
  <div class="field">
    <%= f.label :asked_user_id %><br />
    <%= f.text_field :asked_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
