<%= form_for(@event) do |f| %>

  	<% if @event.errors.any? %>
	   <div id="error_explanation">
	     <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
	     <ul>
	     <% @event.errors.full_messages.each do |msg| %>
	       <li><%= msg %></li>
	     <% end %>
	     </ul>
	   </div>
	 <% end %>
	
	<%-# TODO prepopulate with name %>
	 <div class="name_container">
	   <%= f.label :name %>
	   <%= f.text_field :name, :placeholder => "event name" %>
	 </div>
	 <div class="starttime_container">
		<%= f.label :starttime, 'start date / time'%>
	   <%= f.text_field :starttime %>
	 </div>
	 <div class="endtime_container">
		<%= f.label :endtime %>
	   <%= f.text_field :endtime %>
	 </div>
	<%-# TODO prepopulate with location %>
	<%-# TODO need ajaxy search api for locations / addresses. google? %>
	 <div class="location_container">
	 	<%= f.label :location %>
	   <%= f.text_field :location, :placeholder => "location name... ex. Pete's Coffee" %>
	 </div>
	<div class="address">
		<%= f.label :address %>
		<%= f.text_field :address, :placeholder => "666 Your Mom's House"%>
	</div>
	<div class="city">
		<%= f.label :city %>
		<%= f.text_field :city, :placeholder => "Portland? LA? NY? SF?"%>
	</div>
	<div class="state">
		<%= select(:shipaddress, :state, [ 	
			['State', 'None'],
			['Alabama', 'AL'], 
			['Alaska', 'AK'],
			['Arizona', 'AZ'],
			['Arkansas', 'AR'], 
			['California', 'CA'], 
			['Colorado', 'CO'], 
			['Connecticut', 'CT'], 
			['Delaware', 'DE'], 
			['District Of Columbia', 'DC'], 
			['Florida', 'FL'],
			['Georgia', 'GA'],
			['Hawaii', 'HI'], 
			['Idaho', 'ID'], 
			['Illinois', 'IL'], 
			['Indiana', 'IN'], 
			['Iowa', 'IA'], 
			['Kansas', 'KS'], 
			['Kentucky', 'KY'], 
			['Louisiana', 'LA'], 
			['Maine', 'ME'], 
			['Maryland', 'MD'], 
			['Massachusetts', 'MA'], 
			['Michigan', 'MI'], 
			['Minnesota', 'MN'],
			['Mississippi', 'MS'], 
			['Missouri', 'MO'], 
			['Montana', 'MT'], 
			['Nebraska', 'NE'], 
			['Nevada', 'NV'], 
			['New Hampshire', 'NH'], 
			['New Jersey', 'NJ'], 
			['New Mexico', 'NM'], 
			['New York', 'NY'], 
			['North Carolina', 'NC'], 
			['North Dakota', 'ND'], 
			['Ohio', 'OH'], 
			['Oklahoma', 'OK'], 
			['Oregon', 'OR'], 
			['Pennsylvania', 'PA'], 
			['Rhode Island', 'RI'], 
			['South Carolina', 'SC'], 
			['South Dakota', 'SD'], 
			['Tennessee', 'TN'], 
			['Texas', 'TX'], 
			['Utah', 'UT'], 
			['Vermont', 'VT'], 
			['Virginia', 'VA'], 
			['Washington', 'WA'], 
			['West Virginia', 'WV'], 
			['Wisconsin', 'WI'], 
			['Wyoming', 'WY']]) %>
	</div>
	<div class="zip">
		<%= f.label :zip %>
		<%= f.text_field :zip, :placeholder => "97219"%>
	</div>
	<div class="description_container">
		<%= f.label :description, "description"%>
  		<%= f.text_field :description, :placeholder => "event description..." %>
	</div>
	<%- # TODO change categories to "tags" and get rid of weird shit below %>
	<!--
  <div class="categories"> 
    <%= f.label "Categories" %><br /> 
	  <% for category in Category.all %>
        <%= check_box_tag 'event[category_ids][]', category.id, @event.category_ids.include?(category.id), :id => dom_id(category) %>
        <%= label_tag dom_id(category), category.name, :class => "check_box_label" %> 
	  <% end %>
  </div>
	-->
  <div class="actions">
	<%- # TODO probably change or customize this helper method %>
	<%- # this method is found in app/helper/appication it is a helper method%>
	<!--
    <%= submit_or_cancel(f) %>
	-->
  </div>
<% end %>
